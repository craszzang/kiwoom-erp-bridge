# Telegram 일일 리포트 — 이 파일을 config.telegram.yaml 로 복사 후 값 입력
# 절대 git에 커밋하지 마세요

telegram:
  enabled: true
  bot_token: "YOUR_BOT_TOKEN"
  chat_id: "YOUR_CHAT_ID"
  parse_mode: "HTML"

# 또는 환경변수:
#   TELEGRAM_BOT_TOKEN
#   TELEGRAM_CHAT_ID
